[package]
name = "tribunal"
edition = "2024"
# Tribunal — judicial layer for agent societies on Sui.
# Verifiable, persistent case law on Walrus.

[dependencies]
# Sui framework + MoveStdlib are resolved from the system genesis deps.
# Walrus / Seal package deps are added at the on-chain certification
# integration point; the core lifecycle builds against the framework alone.

[addresses]
tribunal = "0x0"
