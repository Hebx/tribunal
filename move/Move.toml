[package]
name = "tribunal"
edition = "2024"
# Sui Overflow 2026 · Walrus track · Glorian Labs
# Multi-agent AI arbiter with verifiable, persistent memory on Walrus.

[dependencies]
# Sui framework + MoveStdlib are resolved from the system genesis deps
# (sui 1.72.5). Walrus / Seal package deps are added when wiring the
# on-chain certification check (M2) — kept out of M1 so the core
# lifecycle builds against the framework alone.

[addresses]
tribunal = "0x0"
