# Move build artifacts
move/build/
**/build/

# Sui local config / keys (never commit)
*.key
*.keystore
.sui/

# Node / SDK
node_modules/
sdk/dist/

# Deployment outputs (network-specific, regenerated by deploy script)
deployment.*.json

# Scratch / temp
sdk/faucet-tmp.mjs

# Internal planning docs — kept local only, not for the public repo
docs/

# OS / editor
.DS_Store
*.swp
.env
.env.*
